"""Shared pytest configuration.

No environment set-up is needed yet: every setting in `app.core.CONFIG` has
a default, so the app imports cleanly with no `.env` and no running
services. Database and Redis fixtures arrive with S2-INFRA-02 and
S2-INFRA-03.
"""
