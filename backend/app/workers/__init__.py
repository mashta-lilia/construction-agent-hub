"""ARQ background workers — one module per job type (CLAUDE.md §2.2).

Queue configuration and the mail/document/report worker modules land with
S2-INFRA-03 and the tickets that own each job type.
"""
