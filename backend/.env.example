# Copy to .env and adjust for a local run: cp .env.example .env
# Every variable below has a working default, so the skeleton also boots
# with no .env at all. Database, Redis, SMTP and AI credentials are added
# by the tickets that introduce those dependencies.

# cors — comma-separated list of allowed origins
CONF_ORIGINS=http://localhost
